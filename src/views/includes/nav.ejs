<%(function(){%>
    <%if(url === "/login/" || url === "/cadastro/"){%>
    <body>
        <nav>
            <p>Agenda</p>
        </nav>
    <%return%>
    <%}%>
    <%if(url === "/home/createContato/"){%>
    <body>
        <nav>
            <a href="/home/logout">sair</a>
        </nav>
    <%return%>
    <%}%>
    <%if(!user){%>
    <body>
        <nav>
            <p>Agenda</p>
            <a href="/login/">login</a>
        </nav>
    <%return%>
    <%}%>
    <%if(user){%>
    <body>
        <nav>
            <p>Agenda</p>
            <div>
                <a href="/logout/">sair</a>
                <%if(url !== "/home/first/63dc297289382541ec5af632/createContato" && url !== "/home/63dc297289382541ec5af632/homePage/createContato" && url !== `/63dc297289382541ec5af632/criado/${createId}/createContato`){%>
                <a href="<%=url%>createContato"> criar contato</a>
                <%}%>
            </div>
        </nav>
    <%return%>
<%}})()%>
    